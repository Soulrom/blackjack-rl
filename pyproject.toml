[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "blackjack-rl"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
blackjack = "blackjack.main:main"
blackjack-train = "blackjack.train:train"
